version: "3.8"
services:
  account-service:
    build:
      context: .
      dockerfile: account-service/Dockerfile
    image: account-service
    networks:
      - book-store-network

networks:
  book-store-network: